@model PaymentPOGHistoryListViewModel

@{
    Layout = "_AdminLayout";
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Payment Product History Table</h4>
                <div class="table-responsive">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th style="width:60px;"> ID </th>
                                <th> User(Seller) </th>
                                <th> User(Buyer) </th>
                                <th> POG </th>
                                <th> Payment Amount </th>
                                <th> Date </th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.PPOGHs.Count() > 0)
                            {
                                @foreach (var ac in Model.PPOGHs)
                                {
                                    <tr>
                                        <td>@ac.Id</td>
                                        @foreach (var q in Model.Users)
                                        {
                                            if (q.Id == ac.UserIdSell)
                                            {
                                                <td>@q.UserName @q.FirstName @q.LastName</td>
                                                break;
                                            }
                                        }
                                        @foreach (var q in Model.Users)
                                        {
                                            if (q.Id == ac.UserIdBuy)
                                            {
                                                <td>@q.UserName @q.FirstName @q.LastName</td>
                                                break;
                                            }
                                        }
                                        @foreach (var q in Model.POGs)
                                        {
                                            if (q.Id == ac.POGId)
                                            {
                                                @foreach (var w in Model.GNs)
                                                {
                                                    if (w.Id == q.GameNameID)
                                                    {
                                                        <td>@w.GameOfName</td>
                                                        break;
                                                    }
                                                }
                                            }
                                        }
                                        <td>@ac.PaymentBalance</td>
                                        <td>@ac.BuyDate</td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td>
                                        <button type="button" class="btn btn-outline-info btn-fw">Nothing is here</button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>